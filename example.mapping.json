// example command:
// dotrain --batch path/to/this.json compile.batch
{
  "compile": {
    "batch": [
      {
        "input": "./path/to/dotrain1.rain",
        "output": "./path/to/compiled1.json",
        "entrypoints": ["exp1", "exp2"]
      },
      {
        "input": "./path/to/dotrain2.rain",
        "output": "./path/to/compiled2.json",
        "entrypoints": ["main"]
      }
    ]
  }
}
